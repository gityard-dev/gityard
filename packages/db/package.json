{
  "name": "@gityard/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit",
    "db:generate": "echo \"Drizzle schema generation will be enabled when schema objects are added\"",
    "db:migrate": "echo \"Apply SQL files from packages/db/migrations with the configured Postgres connection\""
  },
  "dependencies": {
    "zod": "latest"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
